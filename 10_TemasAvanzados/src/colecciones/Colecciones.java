package colecciones;

public class Colecciones {
    // Algunas permiten elementos duplicados
    // Algunas tienes los elementos ordenado
    // java.util --- Collection and Map
    // -- Collection = "padre" API (conjunto de interfaces y clases relacionado con colecciones)
    // Tienes interfaces hijas: 
    // list (listas, elementos duplicados, ordenados) -- ArrayList
    // set(conjuntos de datos, no elementos duplicados) -- SortedSet
    // -- Map (dictionario) -- hashMap
    // ¡¡¡tenemos la información en forma de llave y valor!!!

}
